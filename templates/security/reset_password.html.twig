{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form)}}
        {{ form_row(form.email) }}
        <button class="btn btn-primary mt-1">Ask for reset password</button>
    {{ form_end(form) }}
{% endblock %}